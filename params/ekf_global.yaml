ekf_filter_node:
  ros__parameters:
    frequency: 20.0
    sensor_timeout: 0.1
    two_d_mode: true

    print_diagnostics: true
    debug: false
    debug_out_file: /home/inaut/Tesis-nico/rat_robot/rat_ws/robot_localization_global_debug.txt

    # Frames
    map_frame: map
    odom_frame: odom
    base_link_frame: base_link
    world_frame: map   # <- IMPORTANTE: en global siempre usamos map

    # Odometry local (ya fusionada por el ekf_local)
    odom0: /odometry/filtered
    odom0_config: [false, false, false,   # x, y, z
                   false, false, true,  # roll, pitch, yaw
                   true, false, false,   # vx, vy, vz
                   false, false, true,  # vroll, vpitch, vyaw
                   false, false, false] # ax, ay, az
    odom0_differential: false
    odom0_relative: false

    # GPS (convertido a odometry por navsat_transform)
    odom1: /odometry/gps
    odom1_config: [true, true, false,
                   false, false, false,
                   false, false, false,
                   false, false, false,
                   false, false, false]
    odom1_differential: false
    odom1_relative: false

    # Opcional: IMU
    imu0: /olive/imu/id001/ahrs
    imu0_config: [false, false, false,
                  true,  true,  true,
                  false, false, false,
                  true,  true,  true,
                  false, false, false]

    imu0_differential: false
    imu0_relative: false

    # Output
    publish_tf: true
    publish_acceleration: false
